{
  "name": "pi-unblock",
  "version": "0.1.0",
  "description": "pi extension: interrupts generation and tool-call loops (exact repeats, near-duplicate thinking, cycles, stagnant results) and applies a default/maximum shell timeout",
  "keywords": ["pi-package", "pi-extension", "pi", "loop", "repetition", "timeout", "guardrail"],
  "license": "MIT",
  "type": "module",
  "files": ["index.ts", "config.ts", "detect.ts", "tools.ts", "timeout.ts", "README.md"],
  "scripts": {
    "test": "node --test index.test.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "pi": {
    "extensions": ["./index.ts"]
  }
}
